server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location /trigger-check/ {
        try_files $uri $uri/ /trigger-check/index.html;
    }

    location = / {
        return 301 /trigger-check/;
    }
}